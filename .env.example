# Copiá este archivo a .env y ajustá los valores. NO subir .env al repo.
DB_NAME=filgrama
DB_USER=filgrama
DB_PASSWORD=cambia_esto
# Para correr la app fuera de Docker:
# DB_URL=jdbc:postgresql://localhost:5432/filgrama
# SERVER_PORT=8080
